import React from "react";
import Header from "../components/Header/Header";

const LogIn = (props) => {
    return (
        <React.Fragment >
            <Header />
        </React.Fragment>
    );
}

export default LogIn;